extends layout
//- script(src="../public/javascripts/sketch.js")

block content
	h1= title
	a(href='/rightofway') NEXT EXAMPLE
	p Another good rule when turning left is to always assume other people are turnig left too, or going straight.
	p In this example the driver in the car of lane 5 waits for the driver in lane 1 to complete their right turn, before making a left.
	p Despite arriving at the same time.


	script(src='/javascripts/twoeven.js')